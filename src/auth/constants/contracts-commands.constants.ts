// Password messages
export const CHECK_PASSWORD_REGEX = /^(?=.*[0-9])([a-zA-Z0-9]+)$/;
export const NO_MATCH_PASSWORD_REGEX_MESSAGE =
	'Password should contain letters (A-z) and numbers(0-9)';

// Email messages
export const INVALID_EMAIL_MESSAGE = 'Invalid email';
